#!/usr/bin/env pwsh
# ck-read-guard: PreToolUse hook for the built-in Read tool (PowerShell).
#
# Native Read is allowed. Exploration discipline is enforced by protocol docs
# and search guards (keyword-map + find-scope), not by blocking Read.

exit 0
